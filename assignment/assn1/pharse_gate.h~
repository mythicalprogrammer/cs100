#ifndef PARSER_H_
#define PARSER_H_

#include<string>
#include<iostream>
#include <fstream>
#include<vector>
#include <math.h>
#include "gate.h"
#include "ginput.h"
#include "goutput.h"
#include "gwire.h"

using namespace std;

class parser_gate
{
  public:

  parser_gate() { 
  this->file = "";
  }

  parser_gate(string file) {
  this->file = file;
 }
/*
  void set_filename(string name);
  void parse_it();
  void parse_aux(string check);
  void put_gate();
  int find_input(char t_input);
  int find_gate(char t_gate);
  void find_gate_input(char t_input, bool t_val);
  void make_circuit();
  void aux();
*/
  private:

  string file;
/*
  vector<ginput> in_array;
  vector<goutput> out_array;
  vector<gate> gate_array;
  vector<gwire> gwire_array;*/
};

#endif /*PARSER_H_*/



